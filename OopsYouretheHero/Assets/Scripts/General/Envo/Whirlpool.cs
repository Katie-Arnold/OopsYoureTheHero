using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Whirlpool : MonoBehaviour
{
    //Idea -> when object tagged 'baot' gets into sphere collider of whirlppol, pull towards transform point 
    //slowly
    //needs some sort of 'itensity'? or speed that it drags. Player will need a way to escape 
    //whirlpool needs a timer for it to disappear 



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
